apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-world-again.fullname" . }}
  labels:
    {{- include "hello-world-again.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-world-again.selectorLabels" . | nindent 4 }}
