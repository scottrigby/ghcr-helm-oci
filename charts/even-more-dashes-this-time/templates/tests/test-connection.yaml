apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "even-more-dashes-this-time.fullname" . }}-test-connection"
  labels:
    {{- include "even-more-dashes-this-time.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "even-more-dashes-this-time.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
